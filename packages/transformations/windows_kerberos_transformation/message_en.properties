title = Windows Kerberos Transformation
description = Created to normalize Windows Kerberos event data, this transformation cleans and formats event_type values by removing unnecessary characters such as curly braces and converting them into a standardized comma-separated format. It improves event readability, consistency, and correlation during authentication and Kerberos-related analysis.
