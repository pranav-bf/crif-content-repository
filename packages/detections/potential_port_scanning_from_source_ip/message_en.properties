title = Potential Port Scanning from External IP
description = Detects when a single external IP address generates more than 20 blocked firewall connection attempts within two minutes, targeting different ports on internal hosts — a strong indicator of reconnaissance or network scanning activity.
