
      title = Suspicious Tooling Staged in User-Writable Directory

      description = Detects a process writing multiple executable or interpreter binaries into user-writable or hidden directories. This behavior is commonly associated with attacker tooling staging prior to execution.

      