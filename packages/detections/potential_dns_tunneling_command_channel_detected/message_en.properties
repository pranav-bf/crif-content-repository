title = Excessive DNS Queries to a Single Domain (Potential DNS Tunneling)
description = Triggers when more than 50 DNS query events for the same domain are observed within 10 minutes — a strong indicator of possible DNS tunneling or command-and-control activity over DNS.
